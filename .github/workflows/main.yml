name: Build & Deploy Spring API

on:
  push:
    branches:
      - main

jobs:
  build-deploy:
    name: Build & deploy Spring boot API
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: SETUP JAVA 21
        uses: actions/setup-java@v3
        with:
          distribution: 'corretto'
          java-version: 21

      - name: Unit Tests
        run: mvn -B test --file pom.xml

      - name: Build the Application
        run: |
          mvn clean
          mvn -B package --file pom.xml

      # Build Docker Image & Publish to DockerHub
      - name: Build Docker Image
        uses: actions/build-push-action@v2
        with:
          context: .
          dockerfile: Dockerfile
          push: false # Uncheck this to 'TRUE' push to Docker Hub
          tags:




