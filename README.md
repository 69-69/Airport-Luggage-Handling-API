
# Airport-Luggage-Handling-API


Assignment:
-----------
The functionalities to demonstrate:
– Successful login by any one of the staff members
– Invalid login by a staff member
– Adding a new flight by the administrator
– Retrieving a flight’s details using the unique flight ID by a staff member
– Viewing all passengers of a flight using the unique flight ID by a staff member
– Adding a new passenger by the administrator
– Viewing all the passengers by the administrator
– Removing a passenger using either ticket number or passenger ID by the administrator
– Retrieving a passenger information using the ticket number by a staff member
– Adding a bag for a passenger by a staff member
– Retrieving a bag using its unique ID by a staff member
– Removing a bag using its unique ID by a staff member
– Changing the location/status of a bag using its unique ID by a staff member
– Changing the status of a passenger using the ticket number by a staff member
– Viewing the gate information using the gate number
– Viewing all the bags at a gate using the gate number or flight ID by a ground staff member

SQL Script:
-----------
create table _user
(
is_active      boolean      not null,
is_first_login boolean      not null,
password       varchar(255) not null,
role           varchar(255) not null,
username       varchar(255) not null
primary key
);

alter table _user
owner to anthonystephena;

create table flight
(
flight_code    varchar(6)   not null
primary key,
created_at     timestamp(6) not null,
updated_at     timestamp(6),
airline_name   varchar(255) not null
unique,
departure_time varchar(255),
destination    varchar(255) not null,
gate           varchar(255) not null
unique,
status         varchar(255),
terminal       varchar(255) not null
);

alter table flight
owner to anthonystephena;

create table message_board
(
is_read         boolean      not null,
created_at      timestamp(6) not null,
id              bigint generated by default as identity
primary key,
updated_at      timestamp(6),
airline         varchar(255) not null,
message         varchar(255) not null,
recipient       varchar(255),
sender_username varchar(255) not null
constraint fkdnsw6bc8ee2dnwcduc73xcsg6
references _user
);

alter table message_board
owner to anthonystephena;

create table passenger
(
flight_code    varchar(6),
identification varchar(6)   not null
primary key,
created_at     timestamp(6) not null,
updated_at     timestamp(6),
ticket_number  varchar(10)  not null
unique,
first_name     varchar(255) not null,
last_name      varchar(255) not null,
status         varchar(255)
);

alter table passenger
owner to anthonystephena;

create table bag
(
flight_code   varchar(6)   not null
constraint fkd9cmmanoe17bm3mpxpt9s7smj
references flight,
created_at    timestamp(6) not null,
id            bigint       not null
primary key,
updated_at    timestamp(6),
ticket_number varchar(10)  not null
constraint fk9mv0aapjdx75b0vnggoghafx3
references passenger (ticket_number),
location      varchar(255) not null,
weight        varchar(255)
);

alter table bag
owner to anthonystephena;

create table user_profile
(
created_at timestamp(6) not null,
id         bigint generated by default as identity
primary key,
updated_at timestamp(6),
airline    varchar(50),
email      varchar(255) not null
unique,
firstname  varchar(255) not null,
lastname   varchar(255) not null,
phone      varchar(255) not null
unique,
username   varchar(255) not null
unique
constraint fkb6qrpvuf5yfayifsqood5cwr0
references _user
);

alter table user_profile
owner to anthonystephena;


Models (Entities):
---------------


@MappedSuperclass
public abstract class BaseEntity {
@CreationTimestamp
@Column(nullable = false, updatable = false)
private LocalDateTime createdAt;

    @UpdateTimestamp
    private LocalDateTime updatedAt;

    @JsonProperty
    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    public LocalDateTime getCreatedAt(){
        return this.createdAt;
    }

    @JsonProperty
    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    public LocalDateTime getUpdatedAt(){
        return this.updatedAt;
    }
}

@Entity
@Table(name = "_user")
public class User {

    @Column(nullable = false)
    private String role;

    @Id
    @Column(unique = true, nullable = false, updatable = false)
    private String username;

    @Column(nullable = false)
    private String password;
    /*
     *  ONE-TO-ONE WITH USER PROFILE
     */
    @OneToOne(
            mappedBy = "user",
            cascade = CascadeType.ALL,
            orphanRemoval = true
    )
    private UserProfile profile;

    /*
     *  ONE-TO-MANY (Sent Messages)
     */
    @OneToMany(mappedBy = "sender", cascade = CascadeType.ALL, orphanRemoval = true)
    @JsonManagedReference("sender-username")
    private List<MessageBoard> sentMessages;
    
    @Column(nullable = false)
    private boolean isFirstLogin;

    public boolean isActive() {
        return isActive;
    }

    public void setActive(boolean active) {
        isActive = active;
    }

    @Column(nullable = false)
    private boolean isActive;
}


@Entity(name = "user_profile")
public class UserProfile extends BaseEntity {
@Id
@GeneratedValue(strategy = GenerationType.IDENTITY)
private Long id;

    @Column(nullable = false, unique = true)
    private String email;

    @Column(nullable = false, unique = true)
    private String phone;

    @Column(nullable = false)
    private String firstname;

    @Column(nullable = false)
    private String lastname;

    @Column(length = 50)
    private String airline;

    @OneToOne
    @JoinColumn(
            name = "username",
            referencedColumnName = "username",
            nullable = false,
            unique = true
    )
    @JsonIgnore
    private User user;
}


@Entity
public class Passenger extends BaseEntity {

    @Id
    @Column(length =  6, nullable = false, unique = true)
    private String identification; // Passport or Driver License Number

    @Column(nullable = false)
    private String firstName;

    @Column(nullable = false)
    private String lastName;

    @Column(length =  10, nullable = false, unique = true)
    private String ticketNumber; // 1234567891

    @Column(length =  6)
    private String flightCode; // AA1234

    private String status;

    // One Passenger have Many Bags
    @OneToMany(
            mappedBy = "passenger",
            cascade = CascadeType.ALL,
            orphanRemoval = true
    )
    @JsonManagedReference("passenger-bags") // This prevents infinite Loop
    private List<Bag> bags;
}


@Entity
public class Passenger extends BaseEntity {

    @Id
    @Column(length =  6, nullable = false, unique = true)
    private String identification; // Passport or Driver License Number

    @Column(nullable = false)
    private String firstName;

    @Column(nullable = false)
    private String lastName;

    @Column(length =  10, nullable = false, unique = true)
    private String ticketNumber; // 1234567891

    @Column(length =  6)
    private String flightCode; // AA1234

    private String status;

    // One Passenger have Many Bags
    @OneToMany(
            mappedBy = "passenger",
            cascade = CascadeType.ALL,
            orphanRemoval = true
    )
    @JsonManagedReference("passenger-bags") // This prevents infinite Loop
    private List<Bag> bags;
}


@Entity
public class Bag extends  BaseEntity {

    @Id // This ID is Generated & Sent from the UI
    @Column(length = 6, nullable = false, unique = true)
    private Long id; // 123456

    @Column(nullable = false)
    private String location; // Bag Status

    private String weight;

    // Bags are linked to a ticket (Many bags can belong to one ticket)
    @ManyToOne
    @JoinColumn(
            name = "ticket_number",       // column in Bag table
            referencedColumnName = "ticketNumber", // property in Passenger entity
            nullable = false
    )
    @JsonBackReference("passenger-bags")
    private Passenger passenger;

    // New here
    @ManyToOne
    @JoinColumn(
            name = "flight_code", // column in Bag table
            referencedColumnName = "flightCode", // property in Flight entity
            nullable = false
    )
    @JsonBackReference("flight-bags")
    private Flight flight;
}



@Entity
@Table(name = "message_board")
public class MessageBoard extends BaseEntity {
@Id
@GeneratedValue(strategy = GenerationType.IDENTITY)
private Long id;

    @Column(nullable = false)
    private String message;

    @ManyToOne
    @JoinColumn(
            name = "sender_username",
            referencedColumnName = "username",
            nullable = false
    )
    @JsonBackReference("message-sender")
    private User sender; // from

    private String recipient; // to

    @Column(nullable = false)
    private boolean isRead;

    @Column(nullable = false)
    private String airline;
}

